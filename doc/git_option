
克隆：git clone https://github.com/zhagnxinbiao2015/venc_lenkeng.git        



推送：	$ git push origin test:master         // 提交本地test分支作为远程的master分支
		$ git push origin test:test              // 提交本地test分支作为远程的test分支
		$ git push origin :test              // 刚提交到远程的test将被删除，但是本地还会保存的，不用担心
	  

拉取：	git fetch origin master 	 //拉取的分区内容部会自动合并
		git pull origin master		 //拉取分支内容并合并

